# Nginx Configuration for 3d.next.lc
# Serves static assets directly, proxies dynamic requests to Next.js
#
# Performance benefits:
# - Static assets bypass Next.js completely
# - Long cache headers for immutable assets
# - Gzip compression for text-based assets
# - CORS headers for cross-origin 3D model loading
#
# Place this file in /etc/nginx/sites-available/ and symlink to sites-enabled/
# Or include it in your main nginx.conf

server {
    listen 80;
    server_name 3d.next.lc;

    # Root directory for static files
    # Adjust this path to match your deployment
    root /var/www/cue-customizer-nextjs/public;

    # Enable gzip for compressible content
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_proxied any;
    gzip_types
        text/plain
        text/css
        text/xml
        text/javascript
        application/javascript
        application/json
        application/xml
        application/xml+rss
        image/svg+xml;

    # ============================================
    # STATIC ASSETS - Serve directly from nginx
    # ============================================

    # GLB 3D Models - Already compressed, immutable
    location /models/ {
        alias /var/www/cue-customizer-nextjs/public/models/;
        
        # Immutable cache - 1 year (files are versioned/hashed)
        add_header Cache-Control "public, max-age=31536000, immutable";
        
        # CORS for Three.js cross-origin loading
        add_header Access-Control-Allow-Origin "*";
        add_header Access-Control-Allow-Methods "GET, OPTIONS";
        add_header Access-Control-Allow-Headers "Origin, Content-Type, Accept";
        
        # GLB files are already compressed, disable gzip
        gzip off;
        
        # Proper MIME type for GLB
        types {
            model/gltf-binary glb;
        }
        
        # Enable sendfile for efficient file transfer
        sendfile on;
        tcp_nopush on;
        tcp_nodelay on;
        
        # Handle OPTIONS preflight requests
        if ($request_method = 'OPTIONS') {
            add_header Access-Control-Allow-Origin "*";
            add_header Access-Control-Allow-Methods "GET, OPTIONS";
            add_header Access-Control-Max-Age 86400;
            add_header Content-Length 0;
            return 204;
        }
    }

    # Textures - Leather, defaults, surfaces
    location /textures/ {
        alias /var/www/cue-customizer-nextjs/public/textures/;
        
        # Long cache - 1 year for immutable assets
        add_header Cache-Control "public, max-age=31536000, immutable";
        
        # CORS for Three.js texture loading
        add_header Access-Control-Allow-Origin "*";
        add_header Access-Control-Allow-Methods "GET, OPTIONS";
        
        # Pre-compressed files if available
        gzip_static on;
        
        # Image MIME types
        types {
            image/jpeg jpg jpeg;
            image/png png;
            image/webp webp;
        }
        
        sendfile on;
    }

    # Surface images
    location /surface/ {
        alias /var/www/cue-customizer-nextjs/public/surface/;
        
        # Medium cache - 1 day (surfaces may change)
        add_header Cache-Control "public, max-age=86400";
        add_header Access-Control-Allow-Origin "*";
        
        sendfile on;
    }

    # Logo
    location = /logo.png {
        alias /var/www/cue-customizer-nextjs/public/logo.png;
        add_header Cache-Control "public, max-age=31536000, immutable";
        sendfile on;
    }

    # ============================================
    # NEXT.JS PROXY - Dynamic requests
    # ============================================

    location / {
        proxy_pass https://3d.next.lc;
        proxy_http_version 1.1;
        proxy_ssl_server_name on;
        
        # WebSocket support for HMR (development)
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        
        # Preserve original host and client info
        proxy_set_header Host 3d.next.lc;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Bypass cache for dynamic content
        proxy_cache_bypass $http_upgrade;
        
        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
        
        # Buffer settings
        proxy_buffering on;
        proxy_buffer_size 4k;
        proxy_buffers 8 4k;
    }

    # Next.js static files - let Next.js handle with its own caching
    location /_next/static/ {
        proxy_pass https://3d.next.lc;
        proxy_http_version 1.1;
        proxy_ssl_server_name on;
        proxy_set_header Host 3d.next.lc;
        
        # Next.js static files are immutable
        add_header Cache-Control "public, max-age=31536000, immutable";
    }

    # API routes - no caching
    location /api/ {
        proxy_pass https://3d.next.lc;
        proxy_http_version 1.1;
        proxy_ssl_server_name on;
        proxy_set_header Host 3d.next.lc;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        # Required by resolveAppBaseUrl(): `Host` above is rewritten to the
        # upstream name, so without this the app sees the address it is bound
        # to (0.0.0.0:3000) and hands out status_url / editor_url links that
        # nobody can open.
        proxy_set_header X-Forwarded-Host $host;

        # The GPU worker POSTs finished renders here. A studio video is tens of
        # MB, and nginx's default cap is 1MB — which rejected the upload with a
        # bare nginx 413 before it ever reached Next.js, so the render burned
        # GPU time and then failed at the last step.
        #
        # 0 disables the check entirely, deliberately: the render-worker upload
        # route is the only large-body endpoint, it authenticates with
        # RENDER_WORKER_SECRET before reading the body, and the sizes here are
        # set by our own encoder rather than by anything a stranger sends. A
        # fixed number would just be a limit to rediscover the next time
        # someone renders at a higher bitrate.
        client_max_body_size 0;
        # A pod on a slow link must not have its upload cut mid-body.
        proxy_read_timeout 600s;
        proxy_send_timeout 600s;
        proxy_request_buffering off;

        # No caching for API
        add_header Cache-Control "no-store, no-cache, must-revalidate";
    }
}

# ============================================
# HTTPS VERSION (if using SSL)
# ============================================
# Uncomment and configure if you have SSL certificates

# server {
#     listen 443 ssl http2;
#     server_name 3d.next.lc;
#
#     ssl_certificate /path/to/fullchain.pem;
#     ssl_certificate_key /path/to/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256;
#     ssl_prefer_server_ciphers off;
#
#     # ... (same location blocks as above)
# }
#
# # Redirect HTTP to HTTPS
# server {
#     listen 80;
#     server_name 3d.next.lc;
#     return 301 https://$server_name$request_uri;
# }
